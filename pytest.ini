[pytest]
testpaths = tests
markers =
    live: tests that hit the deployed site over the network (need SITE_URL in .env)
    db: backup/restore round-trip against a LOCAL MySQL database (needs DB_* in .env and mysql tools; skipped unless DB_HOST is localhost)
