# Secrets / local configuration
.env

# Virtual environments
venv/
.venv/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Database backups (hold real data — never commit)
backup/backups/*.sql
backup/backups/*.sql.gz
