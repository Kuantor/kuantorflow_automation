# Test runner + helpers
pytest
python-dotenv
requests

# Needed to import the KuantorFlow app in the app-level tests
Flask
authlib
mysql-connector-python
beautifulsoup4
lxml

# The report tooling: imported by test_report_render_check.py (md_to_pdf.py
# parses Markdown through md_to_docx.py, hence python-docx), and needed to
# render a verification report into test_reports/.
python-docx
pypdf
